# 待修复问题列表

## 问题 1：会员登录没有正确验证本地存储的用户信息

**症状：**
- 直接点击会员登录，输入之前注册过的用户名密码无法登录
- 没有显示任何登录成功或失败的提示
- 每次都需要重新注册，即使用户已经注册过
- 系统似乎没有获取本地存储的账号密码记忆

**可能原因：**
- DataManager.login() 方法没有正确从 SharedPreferences 读取已保存的用户数据
- _users 数组在应用重启后没有正确加载已注册的用户

**调试方向：**
1. 检查 DataManager.init() 方法中用户数据的加载逻辑
2. 确认 _saveUsers() 和 _loadUsers() 是否正常工作
3. 验证 SharedPreferences 中 'users' 键是否正确保存

---

## 问题 2：会员中心点击购物车会闪退并退出登录

**症状：**
- 登录成功后进入会员中心
- 点击"购物车"按钮
- 应用自动闪退回主界面
- 用户被退出登录（登录状态丢失）

**可能原因：**
- CartPage 导航时可能触发某个异常导致应用重启
- 跳转购物车页面时可能调用了某个未初始化的数据
- 可能是路由导航或页面构建时触发了 DataManager 的重新初始化

**调试方向：**
1. 检查 CartPage 是否已正确定义
2. 查看导航到 CartPage 时是否有异常抛出
3. 检查购物车数据初始化逻辑

---

## 备注

- 日期：2026-04-03
- 应用路径：E:\tile_shop_app
- 主文件：lib\main.dart
- 这些问题需要在下次会话中优先修复
